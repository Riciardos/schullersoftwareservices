micronaut:
  application:
    name: api
  server:
    cors:
      enabled: false
      configurations:
        web:
          allowed-origins:
          - https://schullersoftwareservices.com
          - http://localhost:3000
          allowed-methods:
          - GET
          - POST
          - OPTIONS
          allowed-headers:
          - Authorization
          - Content-Type
  security:
    authentication: bearer
    token:
      jwt:
        signatures:
          jwks:
            google:
              url: https://www.googleapis.com/oauth2/v3/certs
    endpoints:
      logout:
        get-allowed: true
    redirect:
      enabled: false
      unauthorized:
        enabled: false
netty:
  default:
    allocator:
      max-order: 3

